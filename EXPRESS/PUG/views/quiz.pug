extends layout

block content
  h1= title
  if show
    form(method='post')
      p Jaka jest twoja ulubiona pora roku?
      ul
        each item in data
          li
            label=item.title
            input(type='radio' name='quiz' value=item._id)
      input(type='submit' value='Zapisz')
      p Aby zobaczyc wyniki oddaj glos
  else
    h1 Dokonales juz glosu w ankiecie
    p Wyniki:
    ul
    each item in data
      li #{item.title} #{item.vote} (#{Math.round(item.vote/sum*100)} %)  
    p Glosów: #{sum} 

